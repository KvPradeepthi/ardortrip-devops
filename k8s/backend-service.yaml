apiVersion: v1
kind: Service
metadata:
  name: flights-service
  labels:
    app: ardortrip-backend
spec:
  type: ClusterIP
  selector:
    app: ardortrip-backend
  ports:
    - port: 8080
      targetPort: 8080
      name: http
